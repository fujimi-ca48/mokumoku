<%= form_for(Follow.new, html: { method: :post }) do |f| %>
    <div><%= f.hidden_field :following_id, value: @user.id %></div>
    <%= f.submit "フォローする", class: "btn btn-large btn-primary follow-btn" %>
<% end %>
  